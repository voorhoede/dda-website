fragment PartnerLogo on PartnerRecord {
  logo {
    id
    responsiveImage(imgixParams: { fit: crop, w: 200, h: 200, auto: format }) {
      src
      srcSet
      width
      height
      alt
      title
      base64
      bgColor
      sizes
    }
  }
}
