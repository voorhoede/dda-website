fragment PartnerLogo on PartnerRecord {
  id
  logo {
    id
    responsiveImage(imgixParams: { fit: fill, w: 200, h: 200, auto: format }) {
      src
      srcSet
      width
      height
      alt
      title
      base64
      bgColor
      sizes
    }
  }
  page {
    slug
  }
}
