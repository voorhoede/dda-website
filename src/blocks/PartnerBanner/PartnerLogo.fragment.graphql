fragment PartnerLogo on MemberRecord {
  logo {
    id
    responsiveImage(imgixParams: { fit: crop, w: 200, h: 200, auto: format }) {
      src
      srcSet
      width
      height
      alt
      title
      base64
      bgColor
      sizes
    }
  }
}
