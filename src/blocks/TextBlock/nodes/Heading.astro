---
import type { Heading } from 'datocms-structured-text-utils';

interface Props {
  node: Heading;
  baseLevel: 1 | 2 | 3;
}

const { node, baseLevel = 1 } = Astro.props;

const level = Math.min(node.level + baseLevel - 1, 6);

const Tag = `h${level}`;
---
<Tag><slot /></Tag>
