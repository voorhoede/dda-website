query MembersPage($locale: SiteLocale = nl) {
  page: homePage(locale: $locale) {
    _seoMetaTags {
      attributes
      content
      tag
    }
  }
  members: allMembers(locale: $locale) {
    id
    name
    slug
  }
}
