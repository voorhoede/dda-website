query Members (
  $first: IntType!
  $skip: IntType!
  $filter: MemberModelFilter
)  {
  meta: _allMembersMeta(filter: $filter) {
    count
  }
  items: allMembers(
    first: $first
    skip: $skip
    filter: $filter
    orderBy: _createdAt_DESC
  ) {
    name
  }
}
