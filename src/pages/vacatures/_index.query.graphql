#import '@blocks/PageHeader/PageHeader.fragment.graphql'

query VacanciesPage {
  page: vacanciesPage {
    _seoMetaTags {
      attributes
      content
      tag
    }
    header {
      ...PageHeader
    }
  }
}
