---
// Placeholder: this page will be implemented dynamically.
// It must remain server-rendered (not prerendered) so the domain-routing
// middleware can rewrite ai-stages.nl/ → /ai-stages/ without hitting
// the ForbiddenRewrite error.
export const prerender = false;
---
