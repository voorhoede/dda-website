#import '@blocks/PageHeader/PageHeader.fragment.graphql'

query AiInternshipsPage($locale: SiteLocale = nl) {
  page: aiInternshipsPage(locale: $locale) {
    _seoMetaTags {
      attributes
      content
      tag
    }
    header {
      ...PageHeader
    }
  }
}
